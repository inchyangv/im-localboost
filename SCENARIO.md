# 달구벌페이 사용자 시나리오

현재 구현된 제품(웹 `/`, `/merchant`, `/city` + 엔진 + 컨트랙트) 기준으로, 사용자가 실제로 겪을 수 있는 흐름을 전부 정리한 문서입니다. 각 시나리오는 "누가 · 어디서 · 무엇을 하면 · 무엇이 보이는가"로 적고, 근거가 되는 규칙(컨트랙트 순서, 엔진 규칙, 웹 사유 순서)을 함께 표시합니다. 수치는 데모 시드 값(1인 일일 상한 5,000원, 건당 상한 3,000원, 상권 시간당 상한 300,000원, 슬롯 기준 100,000원/시간 × 150%, 보류 지연 60초, 상한 변경 지연 60초)을 전제합니다.

표기: **C** = 컨트랙트 `payWithBoost` 규칙, **E** = 엔진 `/attest` 규칙·모델, **W** = 웹 표시 규칙.

---

## 0-0. 영역 구분 (2026-09-19 개편)

- **소비자 앱** `/`, `/pay`, `/history`, `/map`: 연결한 지갑(Kaia Wallet 등)으로만 결제·조회. 데모 계정 선택기 없음. 지갑 미연결이면 "지갑 연결" 게이트.
- **관리자 도구** `/admin`(대시보드: 전체 결제·위험 로그·엔진 상태), `/admin/merchant`, `/admin/city`: 상단에 데모 계정 선택기. 옛 `/merchant`·`/city`는 리다이렉트.
- 아래 1절의 "소비자 페르소나" 서술은 이제 "연결한 지갑"으로 읽는다. 담합 링 등 데모 보조 기능은 관리자 도구에서 페르소나 키로 실행된다.

## 0. 공통: 진입과 페르소나

| # | 상황 | 보이는 것 |
| --- | --- | --- |
| 0-1 | 아무 화면이나 처음 열기 | 상단 바에 페르소나 선택(소비자 1~5, 가맹점 1~3, 대구시, 은행), 선택된 주소 축약, `chainId`, 엔진 연결 상태 점(초록/빨강). 기본 페르소나는 소비자 1 |
| 0-2 | `/`에서 소비자가 아닌 페르소나(가맹점·대구시·은행) 선택 | "상단에서 소비자 페르소나(소비자 1~5)를 선택하세요." 안내만 표시, 결제 폼 없음 |
| 0-3 | `/merchant`에서 가맹점이 아닌 페르소나 선택 | "상단에서 가맹점 페르소나(가맹점 1~3)를 선택하세요." |
| 0-4 | `/city`에서 대구시·은행이 아닌 페르소나 선택 | "상단에서 대구시 또는 은행 페르소나를 선택하세요." |
| 0-5 | 엔진이 꺼져 있거나 CORS 오류 | 상단 점이 빨강. `/`의 최근 결제·`/city`의 위험 로그·율 표가 마지막 값에서 멈춤. 결제는 `/attest`가 먼저 실패하므로 트랜잭션을 보내지 않고 "결제 실패: …" 표시 (W: 엔진 우선 호출 규칙) |
| 0-6 | RPC가 느리거나 끊김 | 잔액·크레딧이 비어 보이거나 "계산 중…"이 길어짐. 트랜잭션 확인 대기 시간이 길어짐 (Kairos 공개 RPC는 수 초) |
| 0-7 | 페르소나 전환 | 잔액·크레딧·최근 결제가 즉시 그 계정 기준으로 다시 읽힘. 세션에 보관된 "직전 서명"은 페르소나와 무관하게 하나만 유지됨 |

---

## 1. 소비자 (`/`)

### 1.1 화면 구성과 조회

| # | 상황 | 보이는 것 |
| --- | --- | --- |
| 1-1 | 진입 | 왼쪽: 상권 5개 SVG 약도(현재 시간 보너스율을 색·숫자로, 10초 갱신). 오른쪽: 지갑 카드(iMKRW 잔액, 보너스 크레딧), 결제 폼, 최근 결제 10건 |
| 1-2 | 약도에서 상권 클릭 | 가맹점 목록이 그 상권 가맹점으로 좁혀지고 첫 가맹점이 자동 선택됨. "전체 보기"로 해제 |
| 1-3 | 가맹점·금액·크레딧 사용액 입력 | 300ms 뒤 `quoteBoost`로 **예상 보너스**를 온체인 계산해 표시. 0원이면 그 아래 "사유: …"(W 사유 순서 1.4절). 그 아래 "상권 현재 율 · 건당 상한 · 오늘 개인 잔여" |
| 1-4 | 크레딧 사용액 > 보유 크레딧 또는 > 결제 금액 | "크레딧 사용액은 보유 크레딧과 결제 금액 이하여야 합니다." 결제 버튼 비활성 |
| 1-5 | 금액 0 이하 | 결제 버튼 비활성 |
| 1-6 | 최근 결제 | 엔진 `/payments?payer=`에서 읽음. 시각·가맹점·금액·보너스(+N/+0)·트랜잭션 링크(탐색기 URL이 있으면 외부 링크) |

### 1.2 정상 결제

| # | 상황 | 결과 카드 | 근거 |
| --- | --- | --- | --- |
| 1-7 | 율이 게시된 상권의 가맹점에 오늘 처음 결제 (예: 북성로 10%, 10,000원) | 진행 문구 "엔진에 위험 판정과 서명을 요청하는 중…" → "판정 tier 0. payWithBoost 전송 중…" → **결과: 지급**, 보너스 1,000원, tier 0 · score, 트랜잭션 링크. 잔액 −10,000, 크레딧 +1,000. 가맹점 +10,000 | C 5단계 `boost = base × rate / 10000` |
| 1-8 | allowance가 부족한 지갑(시드에서는 MaxUint256이라 보통 발생하지 않음) | 먼저 approve 트랜잭션이 나가고 결과 카드에 "(approve 0x…)" 링크가 함께 표시 | W `ensureAllowance` |
| 1-9 | 크레딧을 일부 써서 결제 (예: 10,000원 중 크레딧 1,000) | 결제 금액 "10,000원 (크레딧 1,000원 사용)". 보너스는 현금분 9,000원 기준(900원). 크레딧 −1,000, 잔액 −9,000 | 불변식 4, C `base = amount − useCredit` |
| 1-10 | 보너스가 건당 상한을 넘는 금액 (예: 15% 상권에 30,000원 → 4,500) | 보너스 3,000원으로 잘림. 결과 카드는 "지급" | C `min(boost, perTxBoost)` |
| 1-11 | 오늘 누적 보너스가 5,000원에 가까움 (예: 잔여 500원인데 1,000원 나올 결제) | 보너스 500원으로 잘림. 미리보기의 "오늘 개인 잔여"가 0원이 됨 | C `personDailyBoost − personDay` |
| 1-12 | 가맹점의 이번 시간 매출이 슬롯 상한(150,000원)에 근접 | 상한까지 남은 금액만 보너스 대상 (예: 남은 5,000원이면 10,000원 결제도 5,000원분만 계산) | C `remainSlot` |
| 1-13 | 상권 시간당 집행이 300,000원에 근접 / 상권 예산이 부족 | 남은 만큼만 지급 | C `zoneHourlyCap − zoneHourSpent`, `zoneBudget` |

### 1.3 결제는 성공하지만 보너스 0원

결제 자체는 절대 막히지 않고(불변식 1) 결과 카드가 **결과: 0원**으로 뜨며 사유가 한 줄 표시됩니다. 미리보기 단계에서도 같은 사유가 먼저 보입니다.

| # | 상황 | 사유 문구 | 근거 |
| --- | --- | --- | --- |
| 1-14 | 같은 가게에서 오늘 이미 보너스를 받음 | "오늘 이미 보너스를 받은 가게" | C `pairDay`, 보너스 > 0 확정 시에만 기록 |
| 1-15 | 결제 금액 전액을 크레딧으로 | "크레딧 결제분에는 보너스가 없음" | 불변식 4 |
| 1-16 | 이 시간에 그 상권의 율이 게시되지 않았거나 0% (예: 들안길 0.0%) | "현재 시간 보너스율 0%" | C `rates[zone][hour]` 없음 → 0 |
| 1-17 | 가맹점이 이번 시간 슬롯 상한을 다 씀 | "가맹점 슬롯 상한 도달" | C |
| 1-18 | 오늘 개인 보너스 5,000원을 다 받음 | "오늘 개인 보너스 한도 소진" | C |
| 1-19 | 상권 시간당 300,000원을 다 씀 | "상권 시간당 한도 소진" | C |
| 1-20 | 상권 예산 0원 (예: 시드 직후 북성로) | "상권 예산 소진" | C |
| 1-21 | 엔진이 tier 2로 판정 | "위험 등급 2: 보너스 미지급 (환류, 모델 점수)" 등 사유 코드 병기. 카운터(`pairDay` 등)는 갱신되지 않으므로 같은 가게에 다시 결제해도 1-14가 뜨지 않음 | E, C 6단계 |
| 1-22 | 여러 사유가 동시에 해당 | 위 표의 순서(tier 2 → 전액 크레딧 → 같은 가게 → 율 0 → 슬롯 → 개인 → 상권 시간 → 예산) 중 첫 번째만 표시 | W 사유 순서 |

### 1.4 보류(tier 1)

| # | 상황 | 보이는 것 | 근거 |
| --- | --- | --- | --- |
| 1-23 | 엔진이 tier 1로 판정(환류·쌍 반복·매출 이탈 규칙 또는 모델 0.5~0.8)한 결제 | **결과: 보류**, 보너스 금액, "보류 ID N", 사유 "보류: 60초 뒤 지급 가능 (환류)". 크레딧은 아직 안 늘고 상권 예산은 이미 차감됨 | C 6단계 Pending 생성 |
| 1-24 | 60초 뒤 누군가 `/city`에서 "지급"을 누름 | 크레딧에 반영. 소비자 화면은 다음 조회 때 크레딧이 늘어 있음 | `release()` 누구나 호출 가능 |
| 1-25 | 은행이 "환수"를 누름 | 크레딧에 반영되지 않고 상권 예산으로 복원. 소비자에게는 별도 알림 없음 (최근 결제에는 원래 boost가 남음) | `clawback()` BANK_ROLE |
| 1-26 | 시드 직후의 신규 지갑으로 담합 패턴 | 규칙은 tier 1이어도 모델이 0.8 이상을 주면 **tier 2**로 상향되어 1-21로 감 (데모에서 기본) | E `max(규칙, 모델)` |

### 1.5 결제 실패 (revert)

"결제 실패: <한국어 메시지> (<에러 이름>)" 형태로 폼 아래에 표시되고 결과 카드는 뜨지 않습니다.

| # | 상황 | 메시지 | 근거 |
| --- | --- | --- | --- |
| 1-27 | 서명 발급 후 120초가 지나서 전송됨 (RPC 지연, 지갑 승인 지연 등) | "서명이 만료되었습니다 (AttestationExpired)" | C 2단계 |
| 1-28 | 같은 서명을 다시 제출 (데모 "서명 재사용 공격") | "이미 사용된 서명입니다 (재사용 차단) (NonceUsed)" | C nonce 소진 |
| 1-29 | 서명 내용과 결제 인자가 다름 (금액·가맹점 변경 후 옛 서명 사용) | "서명 내용이 결제 내용과 일치하지 않습니다 (AttestationMismatch)" | C |
| 1-30 | 엔진의 attester 키가 컨트랙트에 등록된 것과 다름 (배포·엔진 불일치) | "서명자가 등록된 attester가 아닙니다 (BadSigner)" | C |
| 1-31 | personId가 연결되지 않은 지갑 | "등록되지 않은 결제자입니다 (personId 없음) (UnregisteredPayer)" | C 1단계 |
| 1-32 | 비활성 가맹점 | "비활성 가맹점입니다 (MerchantInactive)" | C 3단계 |
| 1-33 | iMKRW 잔액 부족 | "iMKRW 잔액이 부족합니다 (ERC20InsufficientBalance)" | 토큰 |
| 1-34 | allowance 부족인데 approve가 실패 | "iMKRW 사용 승인(allowance)이 부족합니다 (ERC20InsufficientAllowance)" | 토큰 |
| 1-35 | 크레딧 초과 사용이 폼 검증을 우회해 들어옴 | "크레딧 잔액이 부족합니다 (InsufficientCredit)" / "크레딧 사용액이 결제 금액을 초과합니다 (CreditExceedsAmount)" | C 4단계 |
| 1-36 | 가스(KAIA) 부족 | viem의 원문 메시지가 그대로 표시 (매핑 없음) | - |
| 1-37 | 트랜잭션은 성공했는데 Paid 이벤트 파싱 실패 | 노란 카드 "트랜잭션은 성공했지만 Paid 이벤트를 찾지 못했습니다." + 링크 | W |

---

## 2. 가맹점 (`/merchant`)

| # | 상황 | 보이는 것 | 근거 |
| --- | --- | --- | --- |
| 2-1 | 가맹점 1~3 선택 | 가맹점 이름·상권·업종·주소, iMKRW 잔액, "이번 시간 슬롯 게이지"(현재 슬롯 매출 / 슬롯 상한 150,000원, 진행 막대), 수취 내역 최근 50건 (10초 갱신) | 온체인 `slotVolume`, 엔진 `/payments?merchant=` |
| 2-2 | 소비자가 이 가게에 결제 | 몇 초 뒤 잔액과 수취 내역에 반영. 게이지가 결제 금액(크레딧 제외)만큼 참 | C `slotVolume += amount − useCredit` (tier 0·1만) |
| 2-3 | tier 2로 차단된 결제를 받음 | 돈은 정상 수취(잔액 증가). 수취 내역에 보너스 +0으로 표시. 게이지는 변하지 않음 | C 6단계 |
| 2-4 | 게이지가 150,000원에 도달 | 이후 이 시간의 결제는 소비자 쪽에서 "가맹점 슬롯 상한 도달"로 보너스 0원. 다음 정시에 게이지 초기화 | C 슬롯 상한 |
| 2-5 | 엔진이 꺼짐 | 게이지(온체인)는 유지, 수취 내역 위에 빨간 오류 문구 | W |
| 2-6 | 가맹점 4~8 (수취 전용 주소) | 페르소나가 없어 이 화면에서 볼 수 없음. 소비자는 결제할 수 있음 | 시드 규칙 |

---

## 3. 대구시 (`/city`, CITY_ROLE)

### 3.1 상권 현황

| # | 상황 | 보이는 것 |
| --- | --- | --- |
| 3-1 | 진입 | 상권 카드 5개: 현재 율(%), 예산 잔액, 이번 시간 집행 / 300,000 게이지. "epoch N · 10초 갱신" |
| 3-2 | 결제가 일어남 | 해당 상권 예산 잔액 −boost, 이번 시간 집행 +boost (보류 건도 예산은 즉시 차감) |
| 3-3 | 정시가 지남 | 이번 시간 집행이 0으로 초기화, 율은 "다음 시간" 값으로 바뀜 (게시되어 있으면) |

### 3.2 예산 예치

| # | 상황 | 보이는 것 | 근거 |
| --- | --- | --- | --- |
| 3-4 | 상권 선택 + 금액 입력 + "예치" | "처리 중…" → "예치 완료 (tx …)". 카드의 예산 잔액 증가, 대구시 iMKRW 잔액 감소 | `depositBudget` (approve 포함) |
| 3-5 | 대구시 잔액(5,000,000)보다 큰 금액 | "iMKRW 잔액이 부족합니다" 오류 | 토큰 |
| 3-6 | 금액 0 | 버튼 비활성 | W |
| 3-7 | 은행 페르소나로 시도 | 버튼 비활성 (대구시만) | CITY_ROLE |

### 3.3 율 게시

| # | 상황 | 보이는 것 | 근거 |
| --- | --- | --- | --- |
| 3-8 | "율 게시" 클릭 (데모 체크 켜짐) | 엔진이 현재 시간·다음 시간 율을 계산해 oracle 키로 `setRates` 2건 전송. "현재 시간 tx · 다음 시간 tx" 링크, 표(상권·bps·slack·예측 매출·기준). 북성로는 1000(10%) 고정, 나머지는 모델 값 | E 5.3 `bps = clip(round_to_50(k × slack × vulnerability × 10000), 0, 1500)` |
| 3-9 | 데모 체크를 끄고 게시 | 북성로도 모델 값. 슬롯의 10%는 탐색값 {0, 500, 1000, 1500} 중 하나가 나올 수 있음 | E 탐색 |
| 3-10 | 어떤 상권이 0 bps로 나옴 (예: 들안길 0.0%) | 그 시간에는 그 상권 결제가 전부 "현재 시간 보너스율 0%". 모델이 예측 매출 ≥ 기준(slack 0)이거나 탐색값 0을 뽑은 경우이며 고치지 않음 | E |
| 3-11 | "현재 율 다시 읽기" | 엔진 `/rates/current`를 다시 조회만 함 (트랜잭션 없음) | - |
| 3-12 | 엔진의 oracle 계정 가스 부족 / 엔진 다운 | 빨간 오류 문구. 율은 이전 게시분 유지 | - |
| 3-13 | 게시 없이 정시가 지남 | 한 번이라도 "율 게시"를 눌렀다면 엔진 폴러가 정시 경계에서 마지막 overrides(북성로 10% 고정 등)를 그대로 써서 현재·다음 시간을 자동 재게시함. 게시한 적이 없으면 모든 상권 0% → 소비자 보너스 0원. 재게시 실패(RPC 장애 등)는 시간당 1회만 재시도 | E `_auto_republish_hook`, C `BadEpoch`: 현재·다음 시간만 게시 가능 |

### 3.4 위험 판정 로그

| # | 상황 | 보이는 것 |
| --- | --- | --- |
| 3-14 | 진입 | 최근 50건, 5초 갱신: 시각·결제자(축약)·가맹점·금액·tier 배지(0 초록/1 노랑/2 빨강)·score·사유(한국어) |
| 3-15 | 정상 결제 | tier 0, 사유 "—" |
| 3-16 | 환류 뒤 결제 | tier 1 또는 2, 사유 "환류" (+ "모델 점수" 등) |
| 3-17 | 같은 쌍이 7일 내 4회 이상 보너스 결제 | 사유 "쌍 반복" (`pairDay` 때문에 하루 1회만 보너스가 나오므로 데모에서는 4일 이상 걸림) |
| 3-18 | 신규 지갑 5개가 1시간 내 같은 가게 결제 | 5번째부터 tier 2, 사유 "신규 지갑 군집" |
| 3-19 | 가맹점 슬롯 매출이 기준의 3배 초과 | 사유 "매출 이탈" (기준 100,000 → 300,000 초과 시. 슬롯 상한 150,000이 먼저 걸리므로 데모 시드에서는 보너스 대상 금액이 아닌 결제 총액으로만 도달 가능) |
| 3-20 | 결제는 안 했지만 `/attest`만 호출된 건 | 로그에는 뜨지 않음. 로그는 판정 결과 저장 시점 기준이며 결제 성공 여부와 무관하게 `/attest` 호출 시 기록 |

### 3.5 보류 목록 (대구시·은행 공통 열람)

| # | 상황 | 보이는 것 | 근거 |
| --- | --- | --- | --- |
| 3-21 | tier 1 결제 발생 | 5초 내 행 추가: ID·결제자·상권·금액·"지급까지 N초". "지급" 버튼은 N초 동안 비활성, "환수"는 은행만 활성 | C Pending |
| 3-22 | 60초 경과 후 대구시 또는 은행이 "지급" | "지급 완료 #N (tx …)". 행 사라짐, 소비자 크레딧 증가 | `release()` 누구나 |
| 3-23 | 대구시가 "환수" 클릭 | 버튼 비활성 (툴팁 "은행 페르소나에서만 가능") | BANK_ROLE |
| 3-24 | 지급 전에 다른 사람이 이미 처리 | "보류 상태가 아닙니다 (이미 지급 또는 환수됨) (NotPending)" | C |
| 3-25 | 카운트다운 도중 강제로 지급 호출 | "아직 지급 가능 시각이 되지 않았습니다 (NotReleasable)" | C |

### 3.6 상한 제안·적용

| # | 상황 | 보이는 것 | 근거 |
| --- | --- | --- | --- |
| 3-26 | 값 수정 후 "제안" | "제안 완료 (tx …)", "적용 가능까지 60초" 카운트다운. 현행 상한은 아직 그대로 | `proposeCaps` CITY_ROLE |
| 3-27 | 60초 뒤 "적용" (누구나) | "적용 완료". "현재 …" 값이 바뀜. 이후 결제부터 새 상한 적용 | `applyCaps` |
| 3-28 | 60초 전에 "적용" | "상한 변경 대기 시간이 지나지 않았습니다 (CapsNotReady)" | C |
| 3-29 | 제안 없이 "적용" | 버튼 비활성 (또는 CapsNotProposed) | C |
| 3-30 | 최대 율을 낮춘 뒤(예: 800) 율 게시 | 엔진이 온체인 `caps().maxRateBps`를 읽어 계산값·override·탐색값을 전부 그 이하로 자르므로 게시가 성공함. 북성로 10% 고정도 800으로 잘려 8.0%가 됨 (`RateTooHigh`는 발생하지 않음) | E `_chain_max_bps`, C `setRates` |
| 3-31 | 보류 지연을 0으로 적용 | tier 1 보너스가 즉시 지급 가능 상태로 생성됨 | C |
| 3-32 | 은행 페르소나 | 입력·제안 비활성, "적용"만 가능 | W |

### 3.7 시뮬레이션 패널

| # | 상황 | 보이는 것 |
| --- | --- | --- |
| 3-33 | `web/public/sim/results.json`이 있음 | 지표 6개 막대 차트(시나리오 none/A/B/C_on/C_off), 총매출·총 보너스·k 표, "지표 정의" 접기 |
| 3-34 | 결과 파일이 없음 (`make sim` 미실행) | 안내 문구만 표시 |
| 3-35 | 값이 null인 칸 | "해당 없음"으로 표시 (예: none의 예산 1원당 순증 매출) |

---

## 4. 은행 (`/city`, BANK_ROLE)

| # | 상황 | 보이는 것 | 근거 |
| --- | --- | --- | --- |
| 4-1 | 은행 페르소나로 진입 | 대구시와 같은 화면이지만 제목이 "은행 운영 화면". 예산 예치·율 게시·상한 제안은 비활성, 보류 목록의 "환수"가 활성 | 역할 분리 |
| 4-2 | 보류 건 "환수" | "환수 완료 #N (tx …)". 상권 예산 잔액이 보너스만큼 복원, 소비자 크레딧에는 반영되지 않음 | `clawback()`, 불변식 5 |
| 4-3 | 지급 가능 시각이 지난 뒤에도 "환수" | 아직 pending이면 가능 (지급 전까지 언제든) | C "Allowed at any time while still pending" |
| 4-4 | 이미 지급된 건 "환수" | "보류 상태가 아닙니다 (NotPending)" | C |
| 4-5 | 은행이 "지급" | 가능 (누구나 호출 가능) | C |

---

## 5. 데모 보조 (`/city` 하단, 데모 전용)

| # | 상황 | 보이는 것 | 근거 |
| --- | --- | --- | --- |
| 5-1 | "담합 링 실행" | 로그 패널에 순서대로: 소비자 1이 들안길 카페에 정상 결제(tier 0) → 가맹점 2가 소비자 2에게 9,000원 Transfer(환류) → 폴러 적재 대기 3초 → 소비자 2 `/attest` → 결제 → 판정 표시 … 소비자 5까지. 마지막에 "담합 링 종료: 위험 판정 N건, 보류 N건, 차단 N건" | `demo.ts` |
| 5-2 | 결과가 전부 tier 2 (시드 직후 기본) | 소비자 2~5 "차단, 보너스 0원". 위험 로그에 tier 2 · "환류, 모델 점수" 4건, 소비자 5는 "환류, 신규 지갑 군집". 보류 목록은 비어 있음 | E 모델이 신규 지갑 + 환류에 0.9 이상 |
| 5-3 | 일부가 tier 1 (지갑이 오래됐거나 모델 점수가 0.8 미만인 경우) | 해당 건은 "보류, 보너스 N원"으로 보류 목록에 생기고 은행이 환수 가능 | C |
| 5-4 | 같은 날 두 번 실행 | 이미 보너스를 받은 소비자는 "오늘 이미 이 가게에서 보너스를 받아 건너뜀 (pairDay)". tier 2로 차단됐던 소비자는 `pairDay`가 기록되지 않아 다시 결제·재판정됨 | C 6단계 |
| 5-5 | 들안길 율이 0%인 시간에 실행 | 소비자 1의 정상 결제도 보너스 0원("현재 시간 보너스율 0%"). 위험 판정은 율과 무관하게 나옴 | - |
| 5-6 | 가맹점 2 잔액이 36,000원 미만 | 환류 Transfer가 실패하며 로그에 오류. 시드는 500,000원이라 13회 이상 실행해야 도달 | 시드 |
| 5-7 | "서명 재사용 공격" (직전 결제 후 120초 이내) | "재사용이 차단되었습니다: 이미 사용된 서명입니다 (재사용 차단) (NonceUsed)" 초록 박스 + 로그 2줄 | C nonce |
| 5-8 | 직전 결제 후 시간이 지나 보관된 서명의 남은 유효 시간이 30초 미만 | 로그 "보관된 서명이 N초 뒤 만료되어 재사용 전에 새 결제를 만듭니다" → 소비자 1이 북성로 한식당에 1,000원 결제(보너스는 0원일 수 있음) → 그 서명을 즉시 재제출 → `NonceUsed`. 만료 검사가 nonce 검사보다 먼저라 `AttestationExpired`가 나오던 것을 막는 가드 | W `replayLastSignature`, C 2단계 순서 |
| 5-9 | 이 브라우저 세션에서 결제한 적이 없음 | "보관된 서명이 없어 재사용 전에 새 결제를 만듭니다" → 5-8과 같은 흐름으로 `NonceUsed` | W |
| 5-10 | 담합 링 직후 "서명 재사용" | 링은 소비자 1의 첫 결제 서명만 보관하므로 보통 만료 임박 상태이며 5-8 흐름을 탐 | W |
| 5-11 | 새 결제 자체가 실패(잔액·가스 부족 등) | "새 결제에 실패해 재사용을 시연할 수 없습니다: <사유>" 빨간 박스 | W |

---

## 6. 시간·상태 경계

| # | 상황 | 영향 |
| --- | --- | --- |
| 6-1 | KST 자정 경과 | `pairDay`, 개인 일일 한도가 초기화됨. 같은 가게에 다시 보너스 가능. 엔진은 자정 감지 시 `k`를 하루 한 번 보정 (`clamp(planned/actual, 0.5, 1.5)`, 실집행 0이면 1.5) |
| 6-2 | 정시 경과 | 슬롯 게이지·상권 시간당 집행 초기화. 율은 다음 시간 게시분으로 전환, 없으면 0% |
| 6-3 | 컨트랙트 재배포 (`make deploy-*`) | 주소·시드가 새로 생김. 엔진과 웹을 다시 빌드·배포해야 함. 엔진 DB에 옛 기록이 있으면 `/payments`·위험 로그에 섞여 보일 수 있음 |
| 6-4 | 엔진 재시작 | 폴러가 DB의 마지막 블록부터 이어서 읽음. DB가 비어 있으면 `startBlock`부터 다시 읽어 결제 이력을 복구하지만 위험 판정 로그는 복구되지 않음 (`/attest` 시점 기록) |
| 6-5 | 폴러가 뒤처짐 (Kairos 지연) | 방금 한 환류 Transfer를 엔진이 아직 못 봐서 "환류" 규칙이 안 걸릴 수 있음. 데모 링은 3초 대기를 넣어 완화 |

---

## 8. QR 결제 (가맹점 제시형, 달구벌페이)

| # | 상황 | 보이는 것 | 근거 |
| --- | --- | --- | --- |
| 8-1 | 가맹점이 `/merchant` "결제 QR"에서 금액 입력 → "QR 만들기" | QR 이미지(내용은 `<사이트>/pay?m=<가맹점>&a=<금액>&r=<참조 8자>`), 금액, 참조, "취소"·"링크 복사". 배지 "결제 대기 중". 금액 입력은 잠김 | `QrPanel`, SPEC 7.1 |
| 8-2 | 금액이 1,000원 미만 | "결제 금액은 1,000원 이상이어야 해요." 버튼 비활성 | W |
| 8-3 | 손님이 폰 기본 카메라로 QR을 찍음 | 같은 사이트의 `/pay` 링크가 열림 (배포 URL 기준) | QR = URL |
| 8-4 | 손님이 `/` "QR 스캔" 버튼 | 카메라 모달. Chrome/Android·Safari 17+는 기기 내장 인식(`BarcodeDetector`), 그 외는 jsQR. 사각형 안에 QR을 맞추면 자동으로 `/pay?…`로 이동 | `QrScanner` |
| 8-5 | 카메라 권한 거부 / HTTP / 미지원 브라우저 | 안내 문구 + "결제 링크 붙여넣기" 입력으로 대체 | W |
| 8-6 | 다른 사이트 QR·아무 문자열 | "달구벌페이 결제 QR이 아니에요." | `parsePayInput` |
| 8-7 | `/pay` 열림 (소비자 페르소나 또는 연결된 지갑) | 가맹점 이름·상권·업종, 금액(고정), 크레딧 사용 입력, 예상 보너스와 사유(1.3절 순서 그대로), 결제 버튼. 결제 흐름은 `/`와 동일(`/attest` → `payWithBoost`) | 불변식 유지 |
| 8-8 | `/pay`를 가맹점·대구시 페르소나로 열거나 지갑 미연결 | "결제할 지갑을 골라 주세요" 게이트: 지갑 연결 또는 데모 소비자 선택 | `PersonaGate` |
| 8-9 | 연결된 지갑이 미등록(personId 없음)이거나 잔액 0 | 온보딩 카드(은행에서 personId 연결·시작 iMKRW 지급) 표시, 결제 버튼 비활성 | 온보딩(다른 세션 구현) |
| 8-10 | 지갑 네트워크가 앱과 다름 | 경고 + "네트워크 전환". 결제 버튼을 누르면 전환을 먼저 시도 | W |
| 8-11 | 링크의 가맹점이 미등록 / 금액이 형식 오류 | "등록되지 않은 가맹점이에요." / "결제 금액은 1,000원 이상의 정수여야 해요." + 소비자 화면 링크 | `parsePayParams` |
| 8-12 | 결제 완료 | `/pay`에 결과 카드(지급·보류·0원 + 사유). 가맹점 화면은 5초 내 "결제가 들어왔어요 · 금액 · 보너스 · tier · tx" 배지, 배지 "결제 완료", "새 QR 만들기" | 엔진 `/payments?merchant=` 폴링 |
| 8-13 | 같은 금액의 다른 결제가 먼저 들어옴 | 참조는 온체인에 없으므로 QR 생성 이후의 같은 금액 Paid를 완료로 간주함 (데모 한계, 문서화) | W 매칭 규칙 |
| 8-14 | QR 생성 전에 있던 결제 | 생성 시점의 목록을 기억해 두고 제외하므로 매칭되지 않음 | W |
| 8-15 | 가맹점이 "취소" | QR 제거, 폴링 중단, 금액 입력 잠금 해제 | W |

---

## 9. 정산 (가맹점 → 은행, 원화 지급)

| # | 상황 | 보이는 것 | 근거 |
| --- | --- | --- | --- |
| 9-1 | 가맹점 데스크 "정산 요청"에 금액 입력 → 요청 | iMKRW `transfer`(가맹점 → 은행) 트랜잭션. "정산 요청을 보냈어요" + tx, 내 정산 요청 목록에 행 추가. 가맹점 잔액 감소 | `requestSettlement` |
| 9-2 | 보유 iMKRW보다 큰 금액 | "보유 iMKRW 이내로 입력해 주세요." 버튼 비활성 | W |
| 9-3 | 가맹점 아닌 계정 | 입력·버튼 비활성, 안내 문구 | W |
| 9-4 | 은행 `/admin/bank` 정산 데스크 | 요청 누계·지급 완료 누계·미지급(요청−지급) 집계, 요청 표(가맹점 → 은행), 지급 완료 표(은행 → 소각). 엔진 `/transfers` 원장 | `loadSettlementLedger` |
| 9-5 | 은행 "원화 입금 처리" | 미지급 총액을 소각 주소 `0x…dEaD`로 `transfer`. "원화 N원 입금 처리(모의)" + tx. 미지급 0원 | `retireSettled` |
| 9-6 | 미지급 0원 | 버튼 "처리할 요청이 없어요" 비활성 | W |
| 9-7 | 엔진 `/transfers` 미구현·다운 | 경고 "정산 원장을 읽지 못했어요", 집계 0 | W |
| 9-8 | 요청과 지급의 대응 | transfer에는 참조가 없어 건별 매칭 대신 누계 차이(FIFO)로 미지급을 계산함 (프로토타입 단순화) | 설계 |

---

## 7. 이 제품이 다루지 않는 시나리오

- 실제 지갑 연결(MetaMask 등), 지갑 서명 승인 UI: 페르소나 키가 브라우저 번들에 있음 (데모 전용).
- 가맹점 등록·비활성화, personId 연결 변경: 컨트랙트 함수는 있으나(BANK_ROLE) 웹 화면이 없음. 시드 스크립트로만 수행.
- 크레딧 현금화·만료, 가맹점 정산(환전): 없음.
- 율 자동 게시는 "한 번 수동 게시한 뒤 정시마다 같은 overrides로 재게시"하는 데모 가드만 있음. 예산·k 정책에 따른 스케줄링은 없음.
- 영수증 OCR, 실제 iMKRW, KYC, 모바일 앱, 지도, 수수료 대납, 다중서명.
